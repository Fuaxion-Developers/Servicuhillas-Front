import IndexService from '@/components/primary/service/IndexService';
import { Box } from '@mui/material';

const page = () => {
  return (
    <Box>
      <IndexService/>
    </Box>
  );
};

export default page;
